# Code + Governance Audit — 2026-05-05

**HEAD at audit:** `6ac2324` (v2.0.16).

7 findings: F5-1 *required status checks not enabled on main* (High); F3-1 *cargo-deny missing as contributor prereq* + F7-1 *required_signatures: false* (Medium); F1-1 rustfmt drift, F2-1 ShellCheck SC2012, F4-1 `fetch_naive.sh` rewrites JSON timestamp on no-op, F6-1 *repo topics empty* (Low).

`cargo clippy` clean, `cargo audit` clean (72 deps), `cargo test` 130/130 pass. The codebase was in good shape; the findings were all governance-axis (repo settings, lint-floor onboarding, script idempotency).

All findings except F7-1 (deferred pending maintainer signing setup) closed by PRs #9, #10, #11 — see [ADR 0001](../../adr/0001-audit-rules-locked-2026-05-05.md) for the locked branch-protection state.
