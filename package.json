{
  "name": "cool-tunnel-tools",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript+Bun port of the Cool Tunnel maintenance scripts: audit, cut_release, fetch_singbox-core, package_release, preflight, security_check, try_question_ratchet. The .sh shims under scripts/ forward into the Bun ports.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "test": "bun test scripts/"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
